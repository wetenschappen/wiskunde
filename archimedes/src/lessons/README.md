# Lessons README

This file is intentionally minimal.

Lesson and agent guidance now lives in `AGENTS.md` at the repository root.

Use:

- `AGENTS.md` for current architecture and coding conventions
- `src/lessons/_template.js` as a starter template (adapt to real current lesson structure)

# Last session ended here
